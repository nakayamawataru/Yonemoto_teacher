<%= render 'content_header' %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box">
        <div class="box-header">
          <h3 class="box-title">クーポン編集</h3>
        </div>
        <div class="box-body">
          <%= form_for @coupon, method: :put do |f| %>
            <%= render 'form', f: f %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>
