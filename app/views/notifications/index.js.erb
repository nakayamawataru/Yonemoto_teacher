$(".notification-list").html("<%= j render(@notifications) %>");
$("#page-entries-info").html("<%= j (page_entries_info @notifications, entry_name: '項目') %>");
$("#pagination").html("<%= j paginate(@notifications, theme: 'twitter-bootstrap-3', remote: true).to_s %>")
