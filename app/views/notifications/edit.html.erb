<%= render 'content_header' %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box">
        <div class="box-header">
          <h3 class="box-title">お知らせ編集</h3>
        </div>
        <div class="box-body">
          <%= form_for @notification do |f| %>
            <%= render "form", f: f %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</section>
